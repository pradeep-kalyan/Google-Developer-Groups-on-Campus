Django>=4.0,<5.0
django-allauth>=0.51.0
gunicorn>=20.1.0  # If deploying to a platform like Heroku
psycopg2>=2.9  # For PostgreSQL support (replace with mysqlclient or sqlite3 depending on your database)
